<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/todayWeatherFragment">
    <fragment
        android:id="@+id/todayWeatherFragment"
        android:name="com.alexxer.weatherapp.screen.today.TodayWeatherFragment"
        android:label="@string/today"
        tools:layout="@layout/fragment_today_weather" />
    <fragment
        android:id="@+id/forecastWeatherFragment"
        android:name="com.alexxer.weatherapp.screen.forecast.ForecastWeatherFragment"
        android:label="@string/forecast"
        tools:layout="@layout/fragment_forecast_weather" />
</navigation>